#include <stdio.h>
int Main{
  int minimo = Math.abs (A[0]-A[1]);

  for( i=0 ; i<n ; i++ )
  {
    for ( j=0 ; j<n ; j++ )
    {
      if (i!=j)
      {
        distancia = Math.abs (A[0] - A[1]);
        if ( distancia < minimo )
        {
          minimo = distancia
        }
      }
    }
  }
}
